using UnityEngine;

// Add this to the Player object.
// The conveyor writes groundVX while you're on it; it's cleared when you leave.
public class ConveyorRider : MonoBehaviour
{
    public float groundVX;
}
